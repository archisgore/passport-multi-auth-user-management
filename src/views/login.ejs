<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link rel="stylesheet" href="/styles.css">
</head>

<body>
    <div class="container">
        <h1>Login</h1>
        <form action="/auth/login" method="POST">
            <div class="form-group">
                <label for="email">Email:</label>
                <input type="email" id="email" name="email" required>
            </div>
            <div class="form-group">
                <label for="password">Password:</label>
                <input type="password" id="password" name="password" required>
            </div>
            <button type="submit">Login</button>
        </form>
        <div class="social-login">
            <h2>Or login with</h2>
            <p>
                <a href="/auth/login/email">Email Magic Link</a>
            </p>
            <p>
            <% if (process.env.GOOGLE_LOGIN==='true' ) { %> <a href="/auth/google">Google</a>
                <% } %>
            </p>
            <p>
            <% if (process.env.FACEBOOK_LOGIN==='true' ) { %> <a href="/auth/google">Google</a>
                <% } %>
            </p>
            <p>
            <% if (process.env.APPLE_LOGIN==='true' ) { %> <a href="/auth/google">Google</a>
                <% } %>
            </p>
            <p>
            <% if (process.env.TWITTER_LOGIN==='true' ) { %> <a href="/auth/google">Google</a>
                <% } %>
            </p>
        </div>
        <p>Don't have an account? <a href="/auth/signup">Sign up here</a></p>
        <p>Forgot your password? <a href="/auth/forgot-password">Reset here</a></p>
    </div>
</body>

</html>